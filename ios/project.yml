name: Signer
options:
  bundleIdPrefix: lt.turron
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  defaultLocalization: en
settings:
  BASE_CONFIGURATION_PLATFORM: iOS
targets:
  Signer:
    type: application
    platform: iOS
    sources:
      - path: Signer
        excludes:
          - "**/*.md"
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: lt.turron.signer
      TARGETED_DEVICE_FAMILY: "1,2"
      INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
      INFOPLIST_KEY_UILaunchScreen_Generation: YES
      INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
      INFOPLIST_KEY_CFBundleDisplayName: Signer
      INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription: "Save the signed PDF to Photos if needed."
      CODE_SIGN_STYLE: Automatic
      SWIFT_VERSION: "5.0"
      GENERATE_INFOPLIST_FILE: YES
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
